---
description: ZenPlayer 项目上下文与开发规范
alwaysApply: true
---

# ZenPlayer 项目概览

ZenPlayer 是一款 **跨平台应用**，用于浏览、下载和播放佛学视频讲座。使用 **SwiftUI** 构建，支持 macOS、iPhone 和 iPad。

## 技术栈

- **语言**: Swift
- **UI 框架**: SwiftUI（纯 SwiftUI，不混用 AppKit）
- **架构**: MVVM（Models / ViewModels / Views / Services）
- **并发**: async/await
- **网络**: URLSession
- **最低版本**: macOS 13+

## 项目结构

```text
ZenPlayer/
├── ZenPlayerApp.swift          # App 入口（配置 Kingfisher、导航栏样式、iOS 方向控制）
├── ContentView.swift           # 根导航容器（iPhone/macOS: NavigationStack；iPad regular: NavigationSplitView）
├── Models/                     # 数据模型（Codable + Identifiable/Hashable）
│   ├── APIResponse.swift
│   ├── Category.swift          # 一级类目
│   ├── Series.swift            # 二级类目（系列讲座）
│   └── Episode.swift           # 单集与播放列表详情（含播放地址）
├── ViewModels/                 # 视图模型（@MainActor + @Observable）
│   ├── HomeViewModel.swift
│   ├── CategoryDetailViewModel.swift
│   ├── SeriesDetailViewModel.swift
│   ├── PlayerViewModel.swift   # PlaybackContext 定义与播放链路
│   └── DownloadManager.swift   # 下载状态与任务管理（单例）
├── Views/                      # SwiftUI 页面
│   ├── HomeView.swift
│   ├── CategoryDetailView.swift
│   ├── SeriesDetailView.swift
│   ├── PlayerView.swift
│   └── *Row / *Card 组件
├── Services/
│   ├── APIService.swift        # 业务 API 请求（单例）
│   ├── MP3DownloadService.swift
│   ├── M3U8DownloadService.swift
│   ├── AVPlayerDenoiseTapProcessor.swift
│   └── RNNoiseProcessor.swift
└── Utilities/
    ├── LayoutConstants.swift
    ├── OrientationManager.swift
    └── PlatformImage.swift
```

## 页面层级与跳转（已按现代码校正）

- **App 启动链路**: `ZenPlayerApp` -> `ContentView`。
- **根导航容器**:
  - iPhone / macOS：`NavigationStack`。
  - iPad（`horizontalSizeClass == .regular`）：`NavigationSplitView`（侧栏 `HomeView` + detail 占位）。
- **统一值路由注册位置**: `ContentView` 通过 `navigationDestination(for:)` 注册三类路由：
  - `CategoryItem` -> `CategoryDetailView`
  - `SeriesItem` -> `SeriesDetailView`
  - `PlaybackContext` -> `PlayerView`
- **页面跳转链路**:
  1. `HomeView` 中 `NavigationLink(value: CategoryItem)` 进入 `CategoryDetailView`
  2. `CategoryDetailView` 中 `NavigationLink(value: SeriesItem)` 进入 `SeriesDetailView`
  3. `EpisodeRowView` 中 `NavigationLink(value: PlaybackContext)` 进入 `PlayerView`
- **当前无 modal 跳转**: 业务主流程未使用 `sheet` / `fullScreenCover`，以导航栈值路由为主。

## 业务领域

- **一级类目 (Category)**: 顶层分类，如"學佛基礎"
- **二级类目 / 系列 (Series)**: 某一主题的讲座系列
- **单集 (Episode)**: 具体一集视频/音频，包含 mp4/mp3/vod 地址
- **播放上下文 (PlaybackContext)**: `EpisodeItem + serverUrl` 的组合，用于从剧集列表跳转到播放页
- 数据来自 `api.jingzong.net` 的 RESTful 接口，响应格式统一为 `APIResponse<T>`

## 编码约定

- Model 遵循 `Codable`，用 `Identifiable` 和 `Hashable` 支持列表/导航
- ViewModel 使用 `@MainActor` 与 `@Observable`，通过普通属性暴露 UI 状态
- 中文注释说明业务逻辑，代码标识符使用英文
- 网络错误通过 `APIError` 枚举统一处理
- 使用 `MARK:` 注释分隔代码区域
- 下载状态统一由 `DownloadManager` 管理，播放链路优先使用本地已下载文件
